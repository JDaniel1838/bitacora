import React from "react";

const ListEmpty = () => {
  return (
    <div className="w-100 h-100 list-activies-empty">
      <i className="bi bi-emoji-expressionless-fill"></i>
      <h4>Aún no has agregado activades.</h4>
    </div>
  );
};

export default ListEmpty;
